@page
@model Gram.Web.Pages.Events.IndexModel

@{
    ViewData["Title"] = "Events";
}

<h1><a asp-page="Create" title="Create" class="far fa-plus-square nav-link text-dark"></a>Events</h1>
<table class="table table-striped">
    <thead>
        <tr>
            <th class="fit"></th>
            <th>
                @Html.DisplayNameFor(model => model.Entity[0].EventName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entity[0].EventStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entity[0].EventDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Entity[0].Attendees)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Entity) {
        <tr>
            <td class="fit">
                <div>
                    <a asp-page="./Details" asp-route-id="@item.Id" title="Details" class="ga far fa-file-alt"></a>
                    <a asp-page="./Edit" asp-route-id="@item.Id" title="Edit" class="ga far fa-edit"></a>
                    <a asp-page="./Delete" asp-route-id="@item.Id" title="Delete" class="ga far fa-trash-alt"></a>
                </div>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EventName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EventStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EventDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Attendees)
            </td>
        </tr>
        }
    </tbody>
</table>
